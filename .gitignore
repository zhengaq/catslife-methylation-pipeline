# Site/server path profile: environment-specific, never commit
config.site.R

# Data & outputs: never commit (governance + size)
/data/
/output/
*.RDat
*.RData
*.rds
*.txt
*.sav
*.xlsx
*.pdf
*.png
*.csv

# Run logs + orchestrator checkpoints (run_stage5_pipeline.sh writes logs/ and logs/.ckpt/)
/logs/
*.log

# ExperimentHub / AnnotationHub caches
.cache/

# Agent skill lockfile
/skills-lock.json

# R session cruft
.Rhistory
.RData
.Rproj.user/
Rplots.pdf

# renv library (lockfile, if added, IS tracked)
renv/library/
renv/staging/
